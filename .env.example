# ── stdio (npm package) ───────────────────────────────────────────────
# Get your API key from https://app.dragapp.com → Settings → Integrations
DRAG_API_KEY=your-api-key-here

# ── HTTP entry point (hosted remote endpoint) ─────────────────────────
# The HTTP server reads the DragApp token per-request from the Authorization
# header, so DRAG_API_KEY is NOT used here. See DEPLOY.md for details.

# Port the HTTP server listens on (default 3001).
MCP_PORT=3001
# Path the MCP handler answers on. "/" is always accepted too, so this works
# whether NGINX strips the /mcp prefix or passes it through (default /mcp).
MCP_PATH=/mcp
# DragApp API base URL. Defaults to the public edge; point at an internal VPC
# address to avoid hairpinning through the public edge.
DRAG_API_BASE=https://app.dragapp.com

# ── OAuth (lets users connect with just the URL) ──────────────────────
# Public base URL of this MCP endpoint, used in OAuth discovery metadata and
# the endpoint URLs advertised to AI clients.
MCP_PUBLIC_URL=https://app.dragapp.com/mcp
# Secret that signs OAuth client IDs and encrypts authorization codes.
# REQUIRED in production; generate with `openssl rand -hex 32`. All instances
# behind the same URL must share the same value. Without it a random per-boot
# secret is used (logins break on restart, and multi-instance doesn't work).
MCP_OAUTH_SECRET=

# ── Domain verification ───────────────────────────────────────────────
# Challenge string issued by a plugin directory to prove we control the
# domain. Served verbatim, unauthenticated, at
# /.well-known/openai-apps-challenge. Unset means the route is not served.
OPENAI_APPS_CHALLENGE=

# ── Rate limiting (Redis) ─────────────────────────────────────────────
# If REDIS_HOST is unset, rate limiting is disabled (all requests allowed).
# Use the same values the rest of the platform uses for this Redis.
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
# Logical database index (default 0).
REDIS_DATABASE=0
# Set to 1 when the Redis server requires TLS (encryption in transit).
# A TLS-only server refuses plaintext connections, which shows up in the
# logs only as a generic connection failure.
REDIS_TLS=0
# Max requests per window, per token (deploy-time decision).
MCP_RATE_LIMIT=60
# Window length in seconds.
MCP_RATE_WINDOW=60
# On a Redis outage: "true" allows requests (fail-open, default), "false" blocks.
MCP_RATE_LIMIT_FAIL_OPEN=true
